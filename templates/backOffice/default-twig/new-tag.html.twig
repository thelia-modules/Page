{% extends '@BackOfficeDefaultTwig/base.html.twig' %}

{% block title %}{{ 'Create a new tag'|trans({}, 'page.bo.default') }}{% endblock %}
{% block body_testid %}page-new-tag-page{% endblock %}

{% block breadcrumb %}
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin.home') }}">{{ 'Home'|trans({}, 'page.bo.default') }}</a></li>
            <li class="breadcrumb-item"><a href="{{ path('page_list') }}">{{ 'Pages'|trans({}, 'page.bo.default') }}</a></li>
            <li class="breadcrumb-item"><a href="{{ path('page_tag_list') }}">{{ 'Tags'|trans({}, 'page.bo.default') }}</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{ 'Create a new tag'|trans({}, 'page.bo.default') }}</li>
        </ol>
    </nav>
{% endblock %}

{% block content %}
    <div class="container-fluid" data-testid="page-new-tag-content">
        <h1 class="h3 mb-3">{{ 'Create a new tag'|trans({}, 'page.bo.default') }}</h1>

        {% if app.request.query.get('error') %}
            <div class="alert alert-danger">{{ app.request.query.get('error') }}</div>
        {% endif %}

        <div class="card">
            <div class="card-body">
                <form method="post" action="{{ path('page_tag_create_tag_action') }}" data-testid="page-tag-create-form">
                    {% include 'Page/include/tag-form-fields.html.twig' with {
                        form: tag_form,
                        tag_value: '',
                        success_url: path('page_tag_list'),
                        error_url: path('page_tag_new_tag')
                    } only %}
                    <button type="submit" class="btn btn-primary" data-testid="page-tag-create-submit">
                        <i class="bi bi-check2" aria-hidden="true"></i> {{ 'Save'|trans({}, 'page.bo.default') }}
                    </button>
                    <a href="{{ path('page_tag_list') }}" class="btn btn-outline-secondary">{{ 'Cancel'|trans({}, 'page.bo.default') }}</a>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
